# Intentionally minimal.
#
# This previously held "-ignorewarnings -dontwarn -dontnote", Sketchware
# boilerplate that suppressed every diagnostic R8 could raise. With
# minification now enabled, real problems must be visible, so add narrowly
# scoped keep rules here as they are actually needed.
